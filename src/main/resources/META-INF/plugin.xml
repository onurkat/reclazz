<idea-plugin>
    <id>com.onurkat.reclazz</id>
    <name>Reclazz</name>
    <vendor email="onur@onurkat.com" url="https://www.onurkat.com/reclazz">Onur Kat</vendor>

    <description><![CDATA[
        <h2>Hot-Reload for SAP Commerce and Spring, Free and Open Source</h2>
        <p>
            Edit your Java code, build, and the running JVM picks up the change in about
            50ms. No server restart, and the application state you spent five minutes
            setting up survives.
        </p>
        <p>
            <b>Built for SAP Commerce (Hybris), where the restart hurts most.</b>
            Save a *-items.xml or *-beans.xml and Reclazz runs the platform's own code
            generation in the background and reloads the regenerated model and DTO classes,
            instead of ant clean all and a restart. Interceptors re-register, ImpEx can
            auto-import, and the agent installs through the platform's documented append
            properties so it survives ant clean all rather than being wiped by it.
        </p>
        <p>
            It works just as well on any Spring Boot application, and on plain Java through
            a watch directory.
        </p>

        <h3>SAP Commerce</h3>
        <ul>
            <li><b>items.xml and beans.xml</b>: regeneration plus reload of the model and DTO classes, without the full rebuild. A new attribute still needs a database column, so Reclazz reminds you to run Update Running System rather than touching your database itself</li>
            <li><b>Interceptor reload</b>: Validate, Prepare, Load and Remove interceptors re-register</li>
            <li><b>ImpEx auto-import</b>: changed .impex files can be imported on save (opt-in), and one carrying a REMOVE header is refused rather than run against your database</li>
            <li><b>Extension-aware</b>: understands the extension layout, classpath and classloader hierarchy, and covers every live application context rather than only the root one</li>
        </ul>

        <h3>Spring</h3>
        <ul>
            <li><b>Bean refresh</b>: singletons are destroyed and recreated, and references still holding the old instance are re-pointed at the new one</li>
            <li><b>MVC re-scan</b>: mappings are re-registered when a controller changes</li>
            <li><b>Cache eviction</b>: caches for @Cacheable and @CacheEvict classes are evicted</li>
            <li><b>Scheduler and events</b>: @Scheduled tasks and @EventListener methods are re-registered</li>
            <li><b>AOP, @Async and Spring Data</b>: proxy caches cleared, beans re-processed, repositories refreshed</li>
            <li><b>Templates</b>: editing a Thymeleaf or Freemarker template takes effect on the next render, without a restart and without turning caching off.</li>
        </ul>

        <h3>The engine</h3>
        <ul>
            <li><b>Structural changes on a stock JDK</b>: add and remove methods and instance fields, and change annotations, on any JDK 17+. No patched JVM required. An added field is set on objects created after the reload. Added static fields and new enum values need a restart, and Reclazz tells you so rather than leaving you to find out. JetBrains Runtime or DCEVM additionally makes new members visible to reflection</li>
            <li><b>Instant</b>: classes are redefined through the Instrumentation API in milliseconds</li>
            <li><b>One click setup</b>: once enabled, the agent is injected into your run configurations for you</li>
            <li><b>100% local and private</b>: no telemetry, no analytics, no outbound network requests. The one socket it opens is bound to loopback</li>
        </ul>

        <h3>How It Works</h3>
        <ol>
            <li>Reclazz asks once whether to turn on. It ships switched off, because enabling it puts an agent into the JVM that runs your code</li>
            <li>Run your application. The agent is injected automatically (SAP Commerce servers start outside the IDE, so Reclazz offers to install it into the platform properties instead)</li>
            <li>Edit code, build your project. Changes are hot-swapped instantly</li>
        </ol>

        <h3>Requirements</h3>
        <ul>
            <li>IntelliJ IDEA 2023.3+</li>
            <li>JDK 17+</li>
            <li>SAP Commerce 2211 or later for the Hybris-specific features</li>
        </ul>

        <p>
            <a href="https://www.onurkat.com/reclazz">Website</a> |
            <a href="https://github.com/onurkat/reclazz">GitHub</a>
        </p>
    ]]></description>

    <change-notes><![CDATA[
        <h3>1.0.14</h3>
        <ul>
            <li>SAP Commerce: saving a property file applies the changed keys to the running server, the same way the HAC console does. Only the keys whose value differs are applied, and values consumed once at startup are still called out as needing a restart</li>
        </ul>

        <h3>1.0.13</h3>
        <ul>
            <li>Thymeleaf and Freemarker templates reload: editing one takes effect on the next render, with caching left on</li>
            <li>Classes referenced from instrumented bytecode are now instrumented themselves. Frame computation resolved types by loading them, from inside the transform, and a class loaded that way never got the infrastructure it needs to be reloaded. JPA entities were the common case</li>
            <li>A field added by a reload is set on objects created afterwards, and adding a static field no longer throws NoSuchFieldError</li>
            <li>Added static fields and new enum values are reported as needing a restart instead of failing quietly</li>
        </ul>

        <h3>1.0.12</h3>
        <ul>
            <li>Notifications can be dismissed again. A balloon posted while a project was still opening could never start its fade-out timer, so it never faded and its entry could not be removed from the event log</li>
            <li>The JDK capability message is written to the Reclazz tool window instead of arriving as a balloon; it is context rather than an event. A JDK that cannot hot-reload at all still warns</li>
        </ul>

        <h3>1.0.11</h3>
        <ul>
            <li>The reload log no longer reports a failure that did not happen: the JVM's expected refusal to refresh constructor bodies on a class that has gained members is recognised rather than shown as a raw exception</li>
            <li>Reconnecting says what happened instead of only that it was trying, including when no running agent was found</li>
            <li>The introduction no longer says Reclazz is switched off when it is already on for the project</li>
        </ul>

        <h3>1.0.10</h3>
        <ul>
            <li>Fixes SAP Commerce OCC: with the agent attached, every OCC response came back an empty 400. Reclazz's own injected members were visible to reflection, so the JAXB context picked them up and failed to build. They are hidden now</li>
        </ul>

        <h3>1.0.9</h3>
        <ul>
            <li>Switching Reclazz on brings up the tool window straight away instead of after an IDE restart, and the status bar item appears only where Reclazz is enabled and opens the tool window when clicked</li>
        </ul>

        <h3>1.0.8</h3>
        <ul>
            <li>ImpEx auto-import refuses a file containing a REMOVE header rather than running it against your live database; set impexAllowRemove=true to opt in</li>
        </ul>

        <h3>1.0.7</h3>
        <ul>
            <li>Updating the plugin now updates the agent your SAP Commerce server loads; it previously kept the one from when the agent was first installed</li>
        </ul>

        <h3>1.0.6</h3>
        <ul>
            <li>Listing text now leads with SAP Commerce support; no change to the plugin itself</li>
        </ul>

        <h3>1.0.5</h3>
        <ul>
            <li>Annotation changes now take effect on any JDK 17+, no longer needing JetBrains Runtime or DCEVM: editing a @RequestMapping value moves the endpoint</li>
        </ul>

        <h3>1.0.4</h3>
        <ul>
            <li>Now installs on IntelliJ IDEA 2023.3 and later, down from 2024.1</li>
        </ul>

        <h3>1.0.3</h3>
        <ul>
            <li>Contact address now matches the one in SECURITY.md</li>
        </ul>

        <h3>1.0.2</h3>
        <ul>
            <li>The post-install introduction is a notification instead of a dialog shown while the IDE starts, which held the event thread until it was clicked</li>
        </ul>

        <h3>1.0.1</h3>
        <p>Marketplace review fixes. No change to what the plugin does.</p>
        <ul>
            <li>Looks the plugin up through the public PluginManager API rather than an internal one</li>
            <li>Guarantees the packaged agent is the shaded jar, which carries the dependencies it loads at startup</li>
        </ul>

        <h3>1.0.0</h3>
        <p>First public release.</p>
        <ul>
            <li>Hot-swaps Java classes into a running JVM: method bodies, and adding or removing methods and fields on any JDK 17+</li>
            <li>Refreshes Spring beans, re-scans MVC mappings, evicts caches, re-registers scheduled tasks and event listeners</li>
            <li>Purpose-built SAP Commerce support: extension-aware watching, interceptor reload, ImpEx auto-import, items.xml and beans.xml regeneration</li>
            <li>Validated end to end against a live SAP Commerce 2211 server</li>
        </ul>
    ]]></change-notes>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.java</depends>

    <extensions defaultExtensionNs="com.intellij">
        <!-- Settings -->
        <projectConfigurable
                parentId="tools"
                instance="com.onurkat.reclazz.plugin.settings.ReclazzConfigurable"
                id="com.onurkat.reclazz.plugin.settings"
                displayName="Reclazz"/>

        <projectService
                serviceImplementation="com.onurkat.reclazz.plugin.settings.ReclazzSettings"/>

        <applicationService
                serviceImplementation="com.onurkat.reclazz.plugin.settings.ReclazzAppState"/>

        <!-- Agent injection into run configurations -->
        <runConfigurationExtension
                implementation="com.onurkat.reclazz.plugin.agent.AgentInjector"/>

        <!-- Core reload manager service -->
        <projectService
                serviceImplementation="com.onurkat.reclazz.plugin.reload.ReloadManager"/>

        <!-- Tool window -->
        <toolWindow
                id="Reclazz"
                anchor="bottom"
                factoryClass="com.onurkat.reclazz.plugin.ui.ReloadToolWindowFactory"
                icon="/icons/reclazz.svg"
                canCloseContents="false"/>

        <!-- Status bar widget -->
        <statusBarWidgetFactory
                id="ReclazzStatusWidget"
                implementation="com.onurkat.reclazz.plugin.ui.ReloadStatusWidgetFactory"/>

        <!-- Notification group -->
        <notificationGroup
                id="Reclazz"
                displayType="BALLOON"/>
        <!-- The one-time introduction. Sticky so it is still there when the
             user finishes whatever they opened the IDE to do. -->
        <notificationGroup
                id="Reclazz Welcome"
                displayType="STICKY_BALLOON"/>

        <!-- Startup activity -->
        <postStartupActivity
                implementation="com.onurkat.reclazz.plugin.ReclazzStartup"/>
    </extensions>

    <actions>
        <action id="Reclazz.TriggerReload"
                class="com.onurkat.reclazz.plugin.reload.TriggerReloadAction"
                text="Reconnect to Reclazz Agent"
                description="Reconnect to the Reclazz agent">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>
        <action id="Reclazz.AttachToServer"
                class="com.onurkat.reclazz.plugin.agent.AttachAction"
                text="Attach Reclazz to Running Server"
                description="Attach the Reclazz agent to a running JVM">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>
        <action id="Reclazz.Disconnect"
                class="com.onurkat.reclazz.plugin.reload.DisconnectAction"
                text="Disconnect from Reclazz Agent"
                description="Disconnect from the running Reclazz agent">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>
    </actions>

    <projectListeners>
        <listener
                class="com.onurkat.reclazz.plugin.reload.CompilationListener"
                topic="com.intellij.compiler.server.BuildManagerListener"/>
    </projectListeners>
</idea-plugin>
