<?xml version="1.0" encoding="UTF-8"?>
<!--
  Copyright 2026 Onur Kat
  SPDX-License-Identifier: Apache-2.0

  Built with Maven (this repository is otherwise Gradle): the Maven plugin
  tooling generates the plugin descriptor and packages it. From this directory:
  mvn -q install
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.onurkat.reclazz</groupId>
    <artifactId>reclazz-maven-plugin</artifactId>
    <version>1.3.0</version>
    <packaging>maven-plugin</packaging>

    <name>Reclazz Maven plugin</name>
    <description>Wires the Reclazz hot-reload agent into spring-boot:run and the
        test JVM, so a Spring Boot or SAP Commerce project reloads in place with
        no manual -javaagent flag.</description>
    <url>https://reclazz.com</url>

    <licenses>
        <license>
            <name>The Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>onurkat</id>
            <name>Onur Kat</name>
            <url>https://www.onurkat.com</url>
        </developer>
    </developers>

    <scm>
        <url>https://github.com/onurkat/reclazz</url>
        <connection>scm:git:https://github.com/onurkat/reclazz.git</connection>
        <developerConnection>scm:git:ssh://git@github.com/onurkat/reclazz.git</developerConnection>
    </scm>

    <properties>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <mavenVersion>3.9.9</mavenVersion>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.apache.maven</groupId>
            <artifactId>maven-plugin-api</artifactId>
            <version>${mavenVersion}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.maven</groupId>
            <artifactId>maven-core</artifactId>
            <version>${mavenVersion}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.maven.plugin-tools</groupId>
            <artifactId>maven-plugin-annotations</artifactId>
            <version>3.13.1</version>
            <scope>provided</scope>
        </dependency>

        <!-- The agent jar itself, so the Mojo can find it on the plugin's
             classpath and never asks the user for a path. -->
        <dependency>
            <groupId>com.onurkat.reclazz</groupId>
            <artifactId>reclazz-agent</artifactId>
            <version>1.3.0</version>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.10.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-plugin-plugin</artifactId>
                <version>3.13.1</version>
                <configuration>
                    <goalPrefix>reclazz</goalPrefix>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <!--
      Publishing to Maven Central through the Central Portal. Kept in a profile so
      a plain `mvn install` for local use never builds javadoc or asks for a GPG
      key. Release with: mvn -Prelease deploy
      Needs a `central` server (the Central Portal user token) in settings.xml and
      the signing key in the local GPG keyring. autoPublish is false, so the upload
      lands as a validated, pending deployment for a human to publish in the Portal.
    -->
    <profiles>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                        <version>3.3.1</version>
                        <executions>
                            <execution>
                                <id>attach-sources</id>
                                <goals><goal>jar-no-fork</goal></goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <version>3.7.0</version>
                        <configuration>
                            <doclint>none</doclint>
                            <quiet>true</quiet>
                        </configuration>
                        <executions>
                            <execution>
                                <id>attach-javadocs</id>
                                <goals><goal>jar</goal></goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>3.2.7</version>
                        <configuration>
                            <!-- Sign without a prompt. The passphrase comes from
                                 the MAVEN_GPG_PASSPHRASE env var, which release.sh
                                 sets from the same signing.password the Gradle
                                 bundles sign with; loopback pinentry keeps gpg
                                 from popping its own dialog, so the whole release
                                 runs unattended. Without the env var gpg falls
                                 back to prompting, so a by-hand build still works. -->
                            <gpgArguments>
                                <arg>--pinentry-mode</arg>
                                <arg>loopback</arg>
                            </gpgArguments>
                        </configuration>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals><goal>sign</goal></goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>0.5.0</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                            <autoPublish>false</autoPublish>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
